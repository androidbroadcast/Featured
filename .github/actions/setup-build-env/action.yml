name: Setup Build Environment
description: Checkout code, configure Java 17 (Temurin), and set up Gradle with caching

runs:
  using: composite
  steps:
    - uses: actions/setup-java@v5
      with:
        distribution: temurin
        java-version: 17

    - uses: gradle/actions/setup-gradle@v6
