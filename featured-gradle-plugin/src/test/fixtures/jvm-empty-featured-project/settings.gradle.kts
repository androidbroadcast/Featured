// The Featured plugin is injected via GradleRunner.withPluginClasspath().
pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "jvm-empty-featured-project"
