<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".SampleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:exported="true"
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Deeplink entry point guarded by @LocalFlag new_feature_section_enabled.
                featured://sample/new-feature routes to the new feature section.
                The activity checks the flag value at runtime and redirects to the home
                screen if the flag is disabled — ensuring disabled features are
                unreachable even via deeplinks.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="featured"
                    android:host="sample"
                    android:pathPrefix="/new-feature" />
            </intent-filter>
        </activity>

    </application>

</manifest>
